{
  "name": "counterpedia-extension",
  "version": "0.1.0",
  "description": "Counterpedia Chrome extension \u2014 source-match side panel v0.1",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:demo": "npm run build && cp manifest.demo.json dist/manifest.json",
    "build:acquisition-dev": "npm run build && cp manifest.acquisition-dev.json dist/manifest.json",
    "build:authoring-dev": "npm run build && cp manifest.authoring-dev.json dist/manifest.json",
    "test": "vitest run",
    "e2e:draft-gate": "bash scripts/draft-e2e-gate.sh",
    "lint": "tsc --noEmit",
    "package": "npm run build && cd dist && zip -r ../counterpedia-extension.zip ."
  },
  "keywords": [
    "chrome-extension",
    "counterpedia"
  ],
  "author": "thelaplage",
  "license": "ISC",
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "@types/node": "^26.1.2",
    "@vitest/ui": "^4.1.10",
    "jsdom": "^29.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
