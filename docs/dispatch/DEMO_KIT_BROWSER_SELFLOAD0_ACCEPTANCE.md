# DEMO-KIT-BROWSER-SELFLOAD0 acceptance

A fresh physical artifact built from this lane may be called recipient-green only after all of the following are shown from a fresh extraction:

1. ordinary install completes;
2. Start prints `Browser extension binding: READY`;
3. the surviving dedicated-profile Chrome process contains both the exact `--user-data-dir` and exact bundled `--load-extension` flags;
4. Counterpedia is present in `chrome://extensions` without manual Load unpacked;
5. real toolbar -> Connect Local -> Capture yields visible `UNADMITTED`;
6. Draft from retained capture yields proposal-only and `Admission: not performed`;
7. fresh DAGR admission + outcome receipts are attributable to that recipient transaction;
8. Reset remains ownership-scoped.

No result from TEAM r3 is inherited across this repair. Rebuild the physical ZIP before rerunning recipient acceptance.

`AUTHORITY_MOVEMENT=0`
