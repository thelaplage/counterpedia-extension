# DEMO-DRAFT-SOURCE0 — canonical browser capture -> Draft from source journey

**DISPATCH ONLY / DRAFT / DO NOT MERGE AS PRODUCT WORK**

`AUTHORITY_MOVEMENT=0` · proposal-only by construction.

Dispatch base at cut: `counterpedia-extension/main@b01b39b315a0902abdcb47756ee84aeba6dc5778`.

This SHA is only the initial pin. Fresh-read every participating repo and current PR head before execution.

## Mission

Freeze one same-run, current-head browser journey:

`ordinary webpage -> extension observation -> governed capture -> retained bytes -> Draft from source -> Authoring v0.5 proposal handoff -> Counterpedia canonical EntryReadModel/proposal projection -> extension compact preview`

This is an **integration proof over existing owners**. It is not permission to create a second Authoring system, second reader model, second capture registry, or extension-owned semantic result.

The terminal object is a **proposal**. Nothing in this journey confers admission, standing, publication, support truth, verification, or institutional authority.

## Mandatory pre-mutation reconciliation

Before changing extension product code:

1. fresh-read `counterpedia-extension/main` scanner, capture, local companion, Draft-from-source and current reader-consumer code;
2. fresh-read `counterpedia-acquisition/main` capture + retained-read owners;
3. fresh-read `counterpedia-authoring/main` current Composer/content-unit/section-role/completeness/handoff stack;
4. fresh-read `counterpedia/main` canonical proposal/EntryReadModel reader surface consumed by the extension;
5. inspect all open/draft PRs in all four repos touching those seams;
6. classify each overlap as `DISJOINT`, `COMPOSABLE`, `OWNERSHIP_CONFLICT`, `ALREADY_IMPLEMENTED`, or `VACANT`;
7. run the current existing launch/preflight before assuming the journey is broken.

Known dispatch-time posture:

- extension main already contains the real scanner -> canonical CHECK E2E proof (#70); this is `DISJOINT` except for shared browser harness infrastructure;
- extension #59 `EXTENSION-EASY-LAUNCH0` is open and `COMPOSABLE` operational UX only;
- the canonical Draft-from-source direction must remain Authoring handoff -> Counterpedia canonical read model -> extension compact consumer. Do not redeclare the semantic reader in the extension;
- prior work indicates Authoring v0.5 includes producer-owned content-unit identity, explicit role, whole-draft completeness binding, and proposal handoff. Re-verify current bytes rather than relying on this sentence.

## Worktree

```bash
git fetch origin --prune
git worktree add -b feat/demo-draft-source0 \
  "$HOME/Developer/worktrees/counterpedia-convergence0/demo-draft-source0" \
  origin/main
cd "$HOME/Developer/worktrees/counterpedia-convergence0/demo-draft-source0"
```

Participating sibling repos must be checked out at exact fresh pins and recorded in the proof packet. Do not mutate sibling repos inside this worktree. A defect in another owner becomes a separate minimal owner-repo PR.

## First principle: one transaction, one provenance packet

Do not prove each hop in isolation and call that end-to-end.

The acceptance run must retain one machine-readable packet that binds:

- extension commit;
- Acquisition commit;
- Authoring commit;
- Counterpedia commit;
- browser source URL/observation identity;
- exact capture id;
- exact retained-byte digest + byte count;
- Authoring job/package/handoff identity and digests;
- content-unit identities;
- section/application-profile role carriage;
- completeness-binding digest/identity;
- Counterpedia proposal/read-model identity;
- extension-consumed projection identity where current contracts expose it.

The packet must be generated from the real run, not hand-authored afterward.

## Canonical journey

Use a real ordinary browser page whose bytes may lawfully be fetched by current Acquisition policy.

Required sequence:

1. launch the extension through the current canonical demo bootstrap/harness;
2. observe the source page in the real browser;
3. explicitly request capture through the existing extension client path;
4. prove the extension is only the client and Acquisition is the producer;
5. obtain a real CaptureReceipt + retained exact bytes;
6. prove retained read reproduces the captured digest/byte count;
7. explicitly invoke **Draft from source** using the retained-capture path, not a fresh untracked URL refetch disguised as the same operation;
8. run the real Authoring producer path that creates system-owned job/content-unit identities;
9. carry explicit role data from the current application-profile/recipe owner — no title/keyword inference;
10. produce the current proposal package/handoff including whole-draft completeness binding;
11. make the handoff available through the current Counterpedia proposal/read-model composition;
12. consume that canonical Counterpedia model in the extension compact UI;
13. prove the extension does not locally recompute or reinterpret support/admission/template/role semantics.

## Freshness / identity locks

- model/caller cannot choose `content_unit_id`;
- caller cannot hand-manufacture Authoring job/package/handoff digest;
- source URL is not capture identity;
- capture digest is not SourceRecord identity;
- same bytes do not silently collapse documentary identity;
- section role comes from the explicit upstream contract, not prose inference;
- completeness binding is structural closure, not support/truth/completeness-of-the-world;
- Counterpedia reader projection is consumed, not redeclared in extension code.

## Proposal-only lock

The result must remain structurally proposal-only through the entire proof.

Reject or stop if any hop silently claims:

- admitted;
- published;
- standing;
- verified/truthful;
- support established merely because a source was captured;
- authenticated accountable authoring principal unless a real current ingress owner supplies one.

The known authenticated `authoring_actor_ref` / ingress-principal gap does **not** block an honest proposal-only demo. It **does** block any authority-bearing crossing. Do not invent an actor string from Git, session, model, repo, byline, job id, or DAGR operator.

## Reader / UI gate

The extension preview should answer:

- what source was captured;
- what exact retained artifact the draft is based on;
- what draft/proposal was produced;
- where evidence/source handles attach;
- what remains unsupported/unavailable where current model exposes that;
- exact proposal-only status;
- link/open action into the canonical Counterpedia representation.

No generic green success card. No local semantic rewrite.

## Proof bar

Required:

- current exact heads for all four repos;
- one real browser run;
- one real Acquisition capture/receipt/retained-read chain;
- one real Authoring job through current v0.5-equivalent handoff;
- system-owned content-unit identities proven non-caller-selected;
- explicit role carriage proven;
- whole-draft completeness binding proven;
- handoff digest carried into/alongside Counterpedia proposal provenance as current contracts support;
- extension consumes canonical Counterpedia model;
- no second fetch unless current product explicitly labels it as a distinct observation;
- browser screenshots of source, Draft-from-source action, and final compact proposal;
- no secret leakage;
- clean process teardown for locally orchestrated services.

## Repair rule

If the same-run transaction fails:

1. identify the first owning boundary that fails;
2. record exact input/output bytes and error;
3. open **one minimal repair in that owner repo**;
4. do not widen the extension to absorb the missing behavior;
5. rerun the whole transaction after the owner repair, not merely the repaired unit test.

## Terminal

One of:

- `DEMO_DRAFT_SOURCE0_GREEN_CURRENT_STACK`;
- `DEMO_DRAFT_SOURCE0_GREEN_AFTER_MINIMAL_OWNER_REPAIR`;
- `STOP_ACQUISITION_CUSTODY_BOUNDARY`;
- `STOP_AUTHORING_HANDOFF_BOUNDARY`;
- `STOP_COUNTERPEDIA_READER_BOUNDARY`;
- `STOP_OWNERSHIP_CONFLICT`.

No merge, admission, publication, standing, authority crossing, or accountable-actor invention is authorized by this dispatch.